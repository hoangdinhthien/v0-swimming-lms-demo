TÔI CẦN BẠN CHECK VÀ LÀM NHỮNG VIỆC SAU ĐÂY:
- 1. đầu tiên: các table (data-table) trong hệ thống khi manager/staff hover chuột vào các item trong table thì phải highlight item đó lên hoặc là vừa highlight vừa show tooltip click vào để xem chi tiết của item đó (tính luôn cả những trang mà có cột Thao tác trong đó). thêm nữa, hiện tại các data-table đang có 1 logic là khi click row của item đó thì nó sẽ đi đến trang detail, tôi không muốn làm như vậy nữa. thay vì thế như tôi đã có nói, khi manager/staff hover chuột vào item đó thì sẽ highlight background của item đó lên. CLICK VÀO TÊN CỦA ITEM ĐÓ MỚI ĐI TỚI TRANG DETAIL tổng những trang hiện tại đang có table (data-table) là /dashboard/manager/classes, /dashboard/manager/students, /dashboard/manager/instructors, /dashboard/manager/staff, /dashboard/manager/courses, /dashboard/manager/news, /dashboard/manager/pools, /dashboard/manager/applications, /dashboard/manager/transactions (là  orders-api đó)
- 2. vì là đang phân trang (pagination) nên không search trên FE(UI) được nên sử dụng api để search. 
  Find-common: news, pool
