ta cần phải sửa lại logic & thao các của drawer trong trang lịch (manager/calendar) 
- case: thêm thủ công lớp học vào LỊCH (ở tab 'Thêm lớp học' trong drawer)
    . ví dụ 1: đầu tiên sẽ cho chọn lớp học trước -> sau đó đến chọn slot + chạy api fetchSlotDetail (để biết xem trong ngày đó và slot đó có huấn luyện viên nào bận hay không? rồi hiện lên UI huấn luyện viên nào bận và huấn luyện viên nào rảnh trong cái slot detail đó) -> sau đó hiện mục chọn hồ bơi (sử dụng api fetchAvailablePools) -> check xem lớp học đó đã có huấn luyện viên hay chưa? nếu chưa có huấn luyện viên thì cho chọn hlv mà đang availale
    . ví dụ 2: đầu tiên sẽ cho chọn lớp học trước -> sau đó đến chọn slot + chạy api fetchSlotDetail (để biết xem trong ngày đó và slot đó có huấn luyện viên nào bận hay không? rồi hiện lên UI huấn luyện viên nào bận và huấn luyện viên nào rảnh trong cái slot detail đó) -> sau đó hiện mục chọn hồ bơi (sử dụng api fetchAvailablePools) -> check xem lớp học đó đã có huấn luyện viên hay chưa? nếu có huấn luyện viên rồi thì vẫn cho người dùng option thay đổi huấn luyện viên bằng cách chọn hlv mà đang availale

ta cần phải sửa lại logic 'Chọn thời gian học *' (ở step 1 schedule-preview-modal.tsx & ở step 1 của create-classes-batch-modal.tsx): 
- TRÊN UI SẼ CHO CHỌN ÍT NHẤT LÀ 1 SLOT VÀ TỐI ĐA LÀ 2 SLOT
    . ví dụ 1: chọn slot 1 (7:00 -> 7:45) ==> min_time: 7, max_time: 7.45 ==> Khung giờ tự động: 7h - 7h45
    . ví dụ 2: chọn slot 1 (7:00 -> 7:45) và slot 2 (8:00 -> 8:45) ==> min_time: 7, max_time: 8.45 ==> Khung giờ tự động: 7h - 8h45
    . ví dụ 3: chọn slot 5 (11:00 -> 11:45) và slot 8 (15:00 -> 15:45) ==> min_time: 11, max_time: 15.45 ==> Khung giờ tự động: 11h - 15h45


logic tính instructor trống/không trống, chưa có check validate (có thể tái sử dụng step 2 trong modal tạo lớp) 